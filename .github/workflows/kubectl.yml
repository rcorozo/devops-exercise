name: kubectl admin

on:
  workflow_dispatch:
    branches: [ main ]
  #push:
    #branches:
      #- main

jobs:

  deploy:
    runs-on: ubuntu-latest
    steps:
      -
        name: kubectl admin
        uses: actions-hub/kubectl@master
        env:
          KUBE_CONFIG: ${{ secrets.KUBE_CONFIG }}
        with:
          args: get pods
